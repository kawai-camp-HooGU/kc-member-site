// ============================================================
// CsWork：アップロード用テンプレート（REQ-028 → REQ-039 v2）
//
//   画面から「テンプレ」をダウンロードして、そのまま書き換えて上げ直せる形にしておく。
//
//   ⚠️ REQ-039 v2 から、正本は3本の md（Claude セッションが同時に作る）。
//        ops       … ポータル読込用md（運用設定値を同梱する）
//        design    … エージェント指示用md（front matter は kind: agent）
//        watchlist … 要監視顧客CSV
//      DB の kind 値は REQ-028 のまま据え置き、意味だけ読み替えている。
//   ⚠️ 検証（build.ts の validate）を通る最小構成にすること。
//   ⚠️ 認証情報は入れない。テンプレートは全運営ロールがダウンロードできる。
// ============================================================
import type { CsWorkKind } from "./server";

const OPS_TEMPLATE = `---
kind: ops
project: kawai-camp
version: 1.0
updated: 2026-08-27
title: 【事業名】CS運用
funnels: [導線A, 導線B, 共通]
---

# このファイルについて

**人が読むためのファイル。** 導線種別ごとに「概要 → 資料・アカウント → タスク」を持ち、末尾に運用設定値を置く。

- このファイルは **Claude セッションが整形して作る**。人はラフな md を渡すだけでよい
- 対になるファイル：エージェント指示用md（同じ整形で同時に作られる。\`version\` を揃える）
- 記法：\`{{ }}\` は「運用設定値」の項目を参照する。**値そのものは各タスクに書かない**
- \`#\` は導線種別（front matter の funnels と一致させる）。\`# このファイルについて\` と \`# 運用設定値\` だけ例外

# 導線A

## 概要

- **対象**：この導線で追いかける相手
- **ゴール**：どうなったら完了か
- **入口**：どこから入ってくるか
- **滞留の考え方**：どうなったら追いかけるのをやめるか

## 資料・Webページ・アカウント一覧

| 用途 | 参照キー | 備考 |
|---|---|---|
| 例 | \`{{申込管理フォーム}}\` | 運用設定値の links を参照する |

## タスク

### 1. タスク名

- **使用ツール**：ツール名（運用設定値の workflow.ツール順 と揃える）

- **タスク詳細**：何をするか
- **実行条件・分岐**
    - 実行条件：いつ実行するか
    - 条件A → どうする
    - 条件B → どうする
- **案内用テンプレート**

\`\`\`
{{お名前}}さま

本文。画面ではこのブロックをコピーできる。

事務局
\`\`\`

# 導線B

## 概要

- **対象**：
- **ゴール**：

## 資料・Webページ・アカウント一覧

| 用途 | 参照キー | 備考 |
|---|---|---|
| 例 |  |  |

## タスク

### 1. タスク名

- **使用ツール**：ツール名

- **タスク詳細**：
- **実行条件・分岐**
    - 実行条件：
- **案内用テンプレート**：（社内作業のため送信なし）

# 共通

## 概要

- **対象**：導線をまたぐ作業

## 資料・Webページ・アカウント一覧

| 用途 | 参照キー | 備考 |
|---|---|---|
| 例 |  |  |

## タスク

### 1. タスク名

- **使用ツール**：ツール名

- **タスク詳細**：
- **実行条件・分岐**
    - 実行条件：
- **案内用テンプレート**：（社内作業のため送信なし）

# 運用設定値

> 🔒 アカウント情報（ID・パスワード）を含む。共有範囲に注意する。
> ⚙️ 各セクションの \`yaml\` ブロックが正本。タスク側の \`{{ }}\` はここを参照する。
> ⚙️ **この節はエージェントにも同じ内容を渡す。**片方だけ直さないこと。

## 適用範囲

\`\`\`yaml
scope:
  事業・PJ名: 事業名
  対象導線: 導線A／導線B／共通
  委任範囲: 確認・集計・監視・報告まで。送信は人が実施
  エスカレーション先: 担当者名
\`\`\`

## 実行スケジュール

\`\`\`yaml
schedule:
  実行時刻: ["09:30", "13:00", "17:00"]
  成果の置き場: Googleドキュメント（1実行1本・成果物ごとにタブ）
  通知先: Chatwork（ドキュメントのURLを1実行1メッセージ）
workflow:
  ツール順:            # 業務フロー画面の並び。使用ツールの値と揃える
    - ツール名
\`\`\`

## サイト・アカウント

\`\`\`yaml
accounts:
  - 用途: ツール名
    url: https://example.com/
    id: ログインID
    pass: パスワード
\`\`\`

## フォーム・Webページ

\`\`\`yaml
links:
  申込管理フォーム:
    name: 表示名
    url: https://example.com/form
\`\`\`

## チャネル・導線の入口

\`\`\`yaml
channels:
  主要チャネル: LINE
  代替チャネル: メール
\`\`\`

## 判定基準

\`\`\`yaml
thresholds:
  要フォロー: 3        # 最終アクションからの営業日数
  最優先: 7            # 最終アクションからの暦日数
  予定日が未来なら対象外: true
\`\`\`

## 顧客ごとのURLパターン

\`\`\`yaml
customer_url_patterns:
  - name: ポータル顧客ページ（会員）
    種別: 会員
    pattern: https://example.com/ops/members/{顧客ID}
  - name: ポータル顧客ページ（LINE）
    種別: LINE
    pattern: https://example.com/ops/line-customers/{顧客ID}
\`\`\`

## 保管先

\`\`\`yaml
storage:
  保管フォルダ:
    name: 保管先の名前
    url: https://example.com/folder
  実行ドキュメント:
    命名: CS運用_YYYYMMDD_HHMM
    テンプレート: テンプレDocの名前（4タブを持つ。複製して使う）
    月次整理: 月末に YYYY-MM フォルダへ移す
\`\`\`

## ファイル書式（入力契約）

**人が守るのは1つだけ。** \`#\` が導線種別、\`###\` がタスク。それ以外は Claude が整える。

- \`##\` は自由な小見出し。**行頭に空白があっても構わない**
- 顧客へ送る文面は \`\`\` で囲む（画面でコピーできる）
- URL・アカウント・しきい値は \`{{ }}\` で参照する。**実値はこの「運用設定値」だけに置く**
- 任意マーカー（書けば確定・書かなければ推定）：\`@tool\` / \`@when\` / \`@out\` / \`@gate\`
- front matter は Claude が付ける（人が書かなくてよい）
- 要監視顧客CSVの列：\`優先度 / 導線種別 / 段階 / 氏名 / 現況 / 顧客種別 / 顧客ID / LINE名 / メールアドレス / 電話番号 / 予定日 / 監視要件 / 最終アクション日 / 最終アクション内容 / 次アクション予定日 / 次アクション提案 / 関連URL / 備考\`
- 「段階」は個別面談だけが持つ（販促アプローチ① → 販促アプローチ② → 予約済み → 面談後）。他の導線は空でよい
- **滞留判定はCSVに持たせない**（最終アクション日・予定日・判定基準から自動計算）
`;

const AGENT_TEMPLATE = `---
kind: agent
project: kawai-camp
version: 1.0
updated: 2026-08-27
title: 【事業名】CS運用_エージェント指示
schedule: ["09:30", "13:00", "17:00"]
settings_ref: ポータル読込用md の「# 運用設定値」
---

# このファイルについて

**AIエージェントが読むためのファイル。** ポータル読込用mdと同じ整形で同時に作られる。

- **基盤に依存しない書き方にする。**「Claude in Chrome で」ではなく「ブラウザで \`{{まるなげ}}\` を開き」と書く
- **\`{{ }}\` を展開しない。** 実値は運用設定値を見る（この md が漏れても被害を限定するため）
- **人がやるタスクも「実行しない」と書いて必ず含める。** 省くと「案内が送られていない顧客がいる」ことに気づけない

# 0. 実行前提

- 運用設定値を読み、\`{{ }}\` を実値に解決してから始める
- 保管フォルダの直近の実行ドキュメントを読み、件数の前回値と課題の継続件数を控える
- 実行時刻の前後5分は UTAGE を開かない運用になっている（同時ログイン制限）

# 1. 禁止事項（違反したら即中止）

- **顧客への送信を行わない。作るのは下書きまで**
- クレームの一次対応を行わない。検知したら「課題」タブの先頭に出す
- 未確定条件（返金・中途解約・分割・開始日・定員 …）に回答しない
- 判断に迷う条件は勝手に回答せず、\`{{エスカレーション先}}\` へ相談する扱いにする

# 2. 手順（ツール順にまとめる）

## 2-1. ツール名　\`{{参照キー}}\`

### 1. タスク名

- 取得：何を取るか
- 判定：前回値との差を (+N) で出す／しきい値は \`{{判定基準}}\`
- 出力：どのタブへ書くか
- 失敗時：数値を出さず「取得失敗」と理由を残し、課題タブへ起票して次へ進む

### 2. タスク名　← 実行しない（人が送る）

- AI がやること：対象者ごとの下書きを作る
- 人がやること：内容を確認して送信する
- 出力：問合せタブの「次アクション提案」

# 3. 出力

- 保管フォルダのテンプレートDocを**複製**し、名前を \`CS運用_YYYYMMDD_HHMM\` にする
- 4つのタブへ書く：**件数集計／問合せ／要監視顧客／課題**
- 取得できなかった項目は **0 で埋めず「取得失敗」と理由**を書く
- 要監視顧客タブは「前回から変わった行」（新規・段階が上がった・外れた）を先頭に出し、全件表はその下に置く
- **できなかったことは必ず課題タブに書く**（取得失敗・URL未登録・PC停止 …）
- 課題タブには「前回から変わったこと」（新規・解消）と継続件数を書く
- Chatwork へドキュメントのURLを **1実行1メッセージ**だけ送る（件数・要判断・取得失敗・課題の件数＋URL）
`;

const WATCHLIST_TEMPLATE = [
  "優先度,導線種別,段階,氏名,現況,顧客種別,顧客ID,LINE名,メールアドレス,電話番号,予定日,監視要件,最終アクション日,最終アクション内容,次アクション予定日,次アクション提案,関連URL,備考",
  "A,導線A,山田太郎,面談日程を調整中,会員,1234,やまだ,taro@example.com,09000000000,,販促アプローチ中（面談の日程お伺いに未返答）,2026-08-20,事務局：面談予約フォームを案内,2026-08-25,LINEで再案内,,",
  "B,共通,鈴木花子,問い合わせ対応中,LINE,88,はなこ,,,2026-09-01,クレーム（返金の可否について）,2026-08-21,事務局：受領の連絡のみ返信,2026-08-24,回答が出しだい連絡,資料|https://example.com/doc,エスカレーション済み",
  "",
].join("\n");

/** 種別ごとのテンプレート本文とファイル名 */
export function templateFor(kind: CsWorkKind): { filename: string; content: string } {
  if (kind === "watchlist") return { filename: "要監視顧客一覧_テンプレート.csv", content: WATCHLIST_TEMPLATE };
  if (kind === "design")    return { filename: "CS運用_エージェント指示用_テンプレート.md", content: AGENT_TEMPLATE };
  return { filename: "CS運用_ポータル読込用_テンプレート.md", content: OPS_TEMPLATE };
}
