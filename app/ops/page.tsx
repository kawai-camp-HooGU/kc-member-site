import App from "../../app";

// ============================================================
// 運営ゾーンのトップ（Phase 2：入り口分離）
//
//   このページに到達できるのは 管理者 / オペレーター だけ。
//   会員ロールは middleware.ts が / へ 302 で追い出す。
//   （画面側の can() だけに頼らない＝サーバー側で必ず止める）
// ============================================================
export default function OpsPage() {
  return <App zone="ops" />;
}
