-- ============================================================
-- 本文の暗号化保存に伴う索引整理
--   body_text を暗号化保存する方針にしたため、本文の部分一致検索（ILIKE）は
--   無効化した。body_text の trigram GIN 索引は不要になり、書き込みコスト・
--   容量の無駄になるため削除する（件名・宛先の索引は残す）。
-- ============================================================
drop index if exists public.mail_messages_body_trgm;
