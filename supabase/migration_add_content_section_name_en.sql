-- ============================================================
-- コンテンツセクションに「英語名（サイドバー上段ラベル）」を追加
--
--   サイドバーのコンテンツ入口は「英語名（上段）＋日本語名（下段）」の2段表示。
--   これまで英語は "Content" 固定だったため、セクションごとに定義できるようにする。
--     name     … 日本語名（下段・ハブ見出し）＝既存
--     name_en  … 英語名（上段ラベル）。未設定なら "Content" にフォールバック
-- ============================================================
alter table public.content_sections
  add column if not exists name_en text;
