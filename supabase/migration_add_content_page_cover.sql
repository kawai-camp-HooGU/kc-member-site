-- ============================================================
-- コンテンツページに「カバー画像URL」列を追加
--
--   会員のコンテンツ画面を「ハブ（カード一覧）→ ページ内」の
--   ドリルダウン構成にするのに伴い、各ページをカードで見せる際の
--   カバー画像を運営が設定できるようにする項目。任意入力。
--
--   ・値は画像URL（Googleドライブ共有URL等）。表示側で toImageUrl により
--     配信用URLへ正規化する（コンテンツのサムネイルと同じ扱い）。
--   ・未設定なら表示側で赤系グラデーションの既定カバーにフォールバックする。
-- ============================================================
alter table public.content_pages
  add column if not exists cover_url text;
