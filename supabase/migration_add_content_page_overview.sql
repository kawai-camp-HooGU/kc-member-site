-- ============================================================
-- コンテンツページに「概要」列を追加
--
--   会員のコンテンツ画面で、ページ切替タブのすぐ下（抽出フィルターの上）に
--   このページの説明文を表示するための項目。任意入力。
-- ============================================================
alter table public.content_pages
  add column if not exists overview text;
