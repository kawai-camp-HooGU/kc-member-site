-- ============================================================
-- migration_knowledge_status.sql のロールバック
--   ⚠️ 消すとナレッジ管理画面の「取り込み状況」が空表示になる（画面自体は壊れない）。
-- ============================================================
drop function if exists public.knowledge_status(uuid);
